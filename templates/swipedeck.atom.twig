{% if page.header.published %}

{% block content %}
{{ page.content|raw }}
{% if displaypagetitle is defined %}
{% if displaypagetitle %}
  <strong>{{page.title}}</strong><br>
{% endif %}
{% endif %}
{% if displaycontenttitle is defined %}
  {% if displaycontenttitle %}
  {{ page.header.swipe_deck_title }}
    {% if page.header.swipe_deck_pdf_url %}
    (<a href="{{ page.header.swipe_deck_pdf_url }}">Download slides as PDF</a>)
    {% endif %}
  {% endif %}
{% else %}
  {{ page.header.swipe_deck_title }}
  {% if page.header.swipe_deck_pdf_url %}
    (<a href="{{ page.header.swipe_deck_pdf_url }}">Download slides as PDF</a>)
  {% endif %}
{% endif %}
<div class="embed-responsive embed-responsive-16by9">
<figure><iframe width="600" height="338" src="{{ 'https://www.swipe.to/embed/' ~ page.header.swipe_deck_id }}" allowfullscreen></iframe></figure>
</div>
<br>
{% endblock %}

{% endif %}
