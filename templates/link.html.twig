{% block content %}{% if page.header.badge_type is empty %}{% set badge_type = 'secondary' %}{% else %}{% set badge_type = page.header.badge_type %}{% endif %}{% if page.header.link_url is not empty %}<a href="{{ page.header.link_url }}">{{ page.header.link_text }}</a>{% else %}{{ page.header.link_text }}{% endif %}{% if page.header.badge_label is defined and page.header.badge_url is not defined %} <span class="badge badge badge-{{ badge_type }}">{{ page.header.badge_label }}</span>{% endif %}{% if page.header.badge_label is defined and page.header.badge_url is defined %} <a href="{{ page.header.badge_url }}" class="badge badge-{{ badge_type }}">{{ page.header.badge_label }}</a>{% endif %}{% endblock %}
